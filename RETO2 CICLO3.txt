CREATE TABLE Cliente (
	ID NUMBER(10) NOT NULL,
	NAME VARCHAR2(4000) NULL,
	EMAIL VARCHAR2(20) NULL,
	AGE NUMBER(10) NULL,
	PRIMARY KEY (ID));

CREATE SEQUENCE Cliente_seq START WITH 1 INCREMENT BY 1;

CREATE OR REPLACE TRIGGER Cliente_seq_tr
BEFORE INSERT ON Cliente FOR EACH ROW
WHEN (NEW.ID IS NULL)
BEGIN
SELECT Cliente_seq.NEXTVAL INTO :NEW.ID FROM DUAL;
END;


BEGIN
    INSERT INTO Cliente(ID,NAME,EMAIL,AGE) VALUES(:id, :name, :email, :age);
    :status_code:=201;
END;



CREATE TABLE Mensaje (
	ID NUMBER(10) NOT NULL,
	MESSAGETEXT VARCHAR2(4000) NULL,
	PRIMARY KEY (ID));

CREATE SEQUENCE Mensaje_seq START WITH 1 INCREMENT BY 1;

CREATE OR REPLACE TRIGGER Mensaje_seq_tr
BEFORE INSERT ON Mensaje FOR EACH ROW
WHEN (NEW.ID IS NULL)
BEGIN
SELECT Mensaje_seq.NEXTVAL INTO :NEW.ID FROM DUAL;
END;